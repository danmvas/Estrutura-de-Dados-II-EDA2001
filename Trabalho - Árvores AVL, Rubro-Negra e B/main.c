#include "arvoreAVL.c"
#include "arvoreB.c"
#include "arvoreRubroNegra.c"
#include <random>

int main(){

    printf("Geração do código para ")
    // rodando o código todo 10 vezes pra geração de 10 chaves aleatórias
    for (int i = 0; i < 10; i ++){
        printf("aqui vem as coisas!");
    }
    
    return 0;
}
